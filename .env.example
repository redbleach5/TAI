# CodeGen AI - Environment overrides
# Copy to .env and adjust

# LLM provider: "ollama" | "lm_studio"
LLM_PROVIDER=ollama

# Ollama (when LLM_PROVIDER=ollama)
OLLAMA_HOST=http://localhost:11434

# LM Studio / vLLM / LocalAI (when LLM_PROVIDER=lm_studio)
OPENAI_BASE_URL=http://localhost:1234/v1

# Server
PORT=8000

# Logging
LOG_LEVEL=INFO

# Security
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
RATE_LIMIT_PER_MINUTE=100

# RAG: embeddings используют LLM provider (Ollama/LM Studio)
EMBEDDINGS_MODEL=nomic-embed-text

# Web search (@web, Cherry Studio–style): опционально
# SEARXNG_URL=http://localhost:8080
# BRAVE_API_KEY=
# TAVILY_API_KEY=
